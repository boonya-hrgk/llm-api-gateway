<Slide>
  <Box style={{
    width: '100%',
    height: '100%',
    backgroundColor: '#FFFFFF',
    padding: 50,
    display: 'flex',
    flexDirection: 'row',
    gap: 32,
  }}>
    <Box style={{ flex: 1, display: 'flex', flexDirection: 'column' }}>
      <Box style={{ display: 'flex', flexDirection: 'row', gap: 8, alignItems: 'center', marginBottom: 8 }}>
        <Text style={{ fontSize: 14, color: '#0EA5E9', letterSpacing: 2 }}>
          05 落地
        </Text>
        <Box style={{
          backgroundColor: '#F59E0B',
          padding: '3px 10px',
          borderRadius: 10,
        }}>
          <Text style={{ fontSize: 12, color: '#FFFFFF', fontWeight: 'bold' }}>🟡 半实现 + 🛣 路线图</Text>
        </Box>
      </Box>
      <Text style={{
        fontSize: 36,
        fontWeight: 'bold',
        color: '#1F3A8A',
        marginBottom: 8,
      }}>
        ⑤ 用量管控
      </Text>
      <Text style={{
        fontSize: 16,
        color: '#475569',
        marginBottom: 20,
      }}>
        先看见，再守住
      </Text>

      <Box style={{
        backgroundColor: '#ECFDF5',
        padding: 16,
        borderRadius: 8,
        borderLeft: '4px solid #10B981',
        marginBottom: 14,
      }}>
        <Text style={{ fontSize: 15, fontWeight: 'bold', color: '#065F46', marginBottom: 10 }}>
          ✅ 当前已有
        </Text>
        <Box style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
          {[
            '用量统计：日/周/月 + 输入/输出/缓存命中分列',
            '密钥×周期矩阵：哪个部门烧了多少一目了然',
            '手动吊销：发现异常一键 revoke',
            '用量告警：发现"该密钥今日调用数异常"→ 立即重置或吊销',
          ].map((s, i) => (
            <Text key={i} style={{ fontSize: 13, color: '#065F46', lineHeight: 1.6 }}>• {s}</Text>
          ))}
        </Box>
      </Box>

      <Box style={{
        backgroundColor: '#FEF3C7',
        padding: 16,
        borderRadius: 8,
        borderLeft: '4px solid #F59E0B',
      }}>
        <Text style={{ fontSize: 15, fontWeight: 'bold', color: '#92400E', marginBottom: 10 }}>
          🛣 下一步可加（路线图）
        </Text>
        <Box style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
          {[
            '请求限流（rate limit）：单密钥每分钟/小时最大请求数，超限自动 429',
            '每日限额：单密钥每日 token 上限，超限熔断；月度预算告警',
            '审批流：超额使用需要管理员审批',
          ].map((s, i) => (
            <Text key={i} style={{ fontSize: 13, color: '#92400E', lineHeight: 1.6 }}>• {s}</Text>
          ))}
        </Box>
      </Box>

      <Text style={{
        fontSize: 14,
        fontWeight: 'bold',
        color: '#1F3A8A',
        textAlign: 'center',
        marginTop: 16,
        padding: 12,
        backgroundColor: '#F8FAFC',
        borderRadius: 6,
      }}>
        v1 给团队用（统计+手动管控已够用），v2 加自动限流给企业级用
      </Text>
    </Box>

    <Box style={{
      width: 440,
      backgroundColor: '#F1F5F9',
      borderRadius: 12,
      padding: 16,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
    }}>
      <Image
        src="assets/statistics.png"
        style={{ width: '100%', objectFit: 'contain', borderRadius: 8 }}
      />
    </Box>
  </Box>
</Slide>
