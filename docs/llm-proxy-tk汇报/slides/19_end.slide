<Slide>
  <Box style={{
    width: '100%',
    height: '100%',
    backgroundColor: '#1F3A8A',
    padding: 60,
    display: 'flex',
    flexDirection: 'row',
    gap: 40,
  }}>
    <Box style={{ flex: 1.2, display: 'flex', flexDirection: 'column', justifyContent: 'center' }}>
      <Text style={{
        fontSize: 14,
        color: '#0EA5E9',
        letterSpacing: 4,
        marginBottom: 16,
      }}>
        总结
      </Text>
      <Text style={{
        fontSize: 56,
        fontWeight: 'bold',
        color: '#FFFFFF',
        lineHeight: 1.2,
        marginBottom: 24,
      }}>
        把大模型变成<br />公司的"自来水"
      </Text>
      <Text style={{
        fontSize: 22,
        color: '#CBD5E1',
        lineHeight: 1.6,
      }}>
        ——拧开就有，每户有表，坏了能关
      </Text>
    </Box>

    <Box style={{ flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'center' }}>
      <Text style={{
        fontSize: 18,
        color: '#0EA5E9',
        fontWeight: 'bold',
        marginBottom: 16,
      }}>
        下一步建议
      </Text>
      <Box style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
        {[
          { label: '方案 A · 试点部门', desc: '推荐：选 1 个部门用 2 周，收集反馈' },
          { label: '方案 B · 灰度上线', desc: '先发卡给研发部，逐步扩到全公司' },
          { label: '方案 C · 加自动限流', desc: 'v1 跑稳后开发限流/限额能力' },
        ].map((opt, i) => (
          <Box key={i} style={{
            backgroundColor: 'rgba(14, 165, 233, 0.12)',
            border: '1px solid #0EA5E9',
            padding: 14,
            borderRadius: 8,
          }}>
            <Text style={{ fontSize: 15, color: '#FFFFFF', fontWeight: 'bold' }}>{opt.label}</Text>
            <Text style={{ fontSize: 12, color: '#CBD5E1' }}>{opt.desc}</Text>
          </Box>
        ))}
      </Box>

      <Text style={{
        fontSize: 13,
        color: '#0EA5E9',
        marginTop: 24,
        textAlign: 'right',
      }}>
        github.com/boonya-hrgk/llm-api-gateway
      </Text>
    </Box>
  </Box>
</Slide>
