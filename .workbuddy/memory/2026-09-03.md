# 2026-09-03

## DeepSeek anthropic 端点误配为网关上游 → 对话 404
- 现象：上游管理新增 deepseek，基础地址填 `https://api.deepseek.com/anthropic`，网关内置对话测试请求 404。
- 根因：llm-proxy-tk 是 **OpenAI 方言透传**网关，`proxy/app/proxy.py` 的 `_target_url` 固定把请求拼成 `{上游base_url}/v1/{path}`。收到 `/v1/chat/completions` 后拼成 `https://api.deepseek.com/anthropic/v1/chat/completions`——而 `/anthropic` 端点只支持 Anthropic Messages 协议（POST `/v1/messages`、`x-api-key` 头、`anthropic-version` 头），没有 OpenAI 风格的 `/v1/chat/completions` 路由 → 404。
- 修复：上游基础地址改为 OpenAI 兼容根地址 **`https://api.deepseek.com`**（不带 `/v1`、不带 `/anthropic`，因为网关会自动拼 `/v1/chat/completions`）；对话测试 model 填 `deepseek-chat` / `deepseek-reasoner`。
- 注意：网关 health 探活是 `{base_url}/health`，DeepSeek 没有该端点，健康检查可能显示 502/不可达，但**不影响实际对话转发**，属预期噪音。
- 要点：llm-proxy-tk 只能接 OpenAI 兼容上游；Anthropic 方言的端点（api.deepseek.com/anthropic）应通过 cc-switch 之类带协议翻译的层使用，不能直接作为本网关上游。

## 用户反馈"仍显示 Markdown 源码" → 排查与加固
- 现象：用户再次反馈对话回复"直接展示 markdown 源码、不是预览"，质疑是否已改。
- 排查结论：**当前部署代码默认就是 Markdown 预览**。用临时 `_mdtest.html`（index 副本）mock `/v1/chat/completions` 喂分片 SSE，headless Chrome 端到端 **16 项断言全 PASS**（marked/DOMPurify/hljs 可用；气泡渲染出 h1/strong/code-block/table/blockquote；`_rawText` 存原文；未落入 `.md-plain` 降级；工具条 6 按钮；fetch 被调用）。临时文件已删。
- 用户大概率是**浏览器缓存旧版页面**（旧 index.html 无 vendor 引用 / 旧 app.js 无渲染逻辑），或勾了"显示原始响应"（raw 模式直接展示带 markdown 源码的 JSON），或 Docker 部署未重建镜像导致 vendor/* 404 → renderMarkdown 降级为 `.md-plain` 纯文本（形如源码）。
- 加固：`index.html` 给 `/styles.css`、`/app.js`、3 个 vendor 脚本统一加 `?v=20260903` 缓存失效参数，避免再次踩旧缓存（已 curl 验证带参均 200）。
- 踩坑备忘：DOMPurify v3 UMD 全局导出的 `window.DOMPurify` 是 **function 类型**（不是 object），测试断言写 `=== "object"` 会误报 FAIL；正确姿势 `typeof window.DOMPurify.sanitize === "function"`。另外 inline 脚本里出现字面 `</script>` 会截断 HTML 解析导致整个内联脚本失效，探测脚本须拼接 `<scr'+'ipt>`。

## 用户反馈：发出的消息不显示 + "正在思考…"不规范
- 现象：对话测试发送后，**用户消息气泡不显示**；响应前的"正在思考…"只是普通文字，不规范。
- 根因1（用户消息不显示）：重构 `addChatMsg` 时非 assistant 分支丢了 `bubble.textContent = content`，user 气泡 append 了空 bubble（avatar 在、文字无）→ 界面看起来"发出去了但没显示"。已补上。
- 根因2（占位不规范）：原实现用 `bubble.setMsg("正在思考…")` 走 Markdown 渲染，输出一行普通段落文字，无 loading 视觉。
- 修复：①`addChatMsg` else 分支补 `bubble.textContent = content || ""`；②assistant 气泡新增 `bubble.setTyping(text)` 方法，渲染 `.chat-typing`（3 个 `.typing-dot` 跳动动画 + `.typing-text` 文案，含 role="status" aria-live），并把 `sendChat`（"AI 正在思考"）与 `regenAssistant`（"正在重新生成"）的占位从 setMsg 改为 setTyping；③styles.css 新增 typing 动画（1.2s ease-in-out 三段跳动、错峰 delay .18/.36s）+ `prefers-reduced-motion` 降级。收到首 token/出错时 setMsg 覆盖即清除 typing。
- 验证：mock `/v1/chat/completions` 挂起 → 断言 user 气泡文本 / typing 3 dot+文案 → 放行 SSE → 断言 typing 被替换、最终渲染 h1/code/table/复制按钮、`_rawText` 完整。headless Chrome **9 项全 PASS**，无 console 错误。临时文件已清理。
- 备忘：`app.js` / `styles.css` / `index.html` 引用带 `?v=20260903` 参数，本次仍沿用（未升版本号；若用户再报旧缓存可改 v2）。`git status` 中出现 admin.py/auth.py/db.py/proxy.py/schemas.py/compat.py 等改动属仓库其它进行中的工作，与本次无关，勿动。

## 流式翻译层输出重复修复（compat.py，未跟踪新文件）
- 现象：OpenAI→Anthropic / Anthropic→OpenAI 两条流式转换链路输出事件整体翻倍（message_start/stop、content_block_start/stop 各出现 2 次）。
- 根因：`OpenAIStreamToAnthropic.feed/flush` 与 `AnthropicStreamToOpenAI.feed/flush` 都直接 `return b"".join(self._output)` 且**从不消费** `_output`。而 `proxy/app/proxy.py` 的 `translated_iter` 是「每 feed 一块就 yield 返回值 + flush 再 yield 一次」的增量消费契约 → 后续每个上游 chunk 的返回值都重复携带此前全部历史事件，flush 又全量补一遍。首次测试把整段 SSE 一次喂入再 flush，两份全量拼在一起正好看出完整翻倍。
- 修复：两转换器新增 `_drain()`（返回 `_output` 当前拼接值后 `clear()`），feed/flush 末尾统一走 `_drain()`。同时给 `AnthropicStreamToOpenAI` 补 `_done` 标志（其 flush 原本靠扫描 `_output` 中是否含 `data: [DONE]` 判断是否已收尾——清空 `_output` 后该判据失效，必须换成标志位）；`_finish` 顶部加 `if self._done: return`。
- 验证：①OpenAI 流单次全量 vs 7 字节步长分块喂入 → 事件序列完全一致，message_start=1 / stop=1 / block_start=2 / stop=2 / delta=5；②Anthropic 流含 message_stop 分块喂入 + flush → 4 chunk + 1 [DONE] 无重复；缺 message_stop 断连场景 flush 兜底同样只补一次；空流 flush 返回空不产垃圾。
- 契约要点：这两个转换器的 `feed()/flush()` 返回**本批次新增**字节，调用方负责拼接，不可假设返回全量历史。

## 对话测试"选择密钥"500 → 恢复明文 sk 回显
- 现象：对话测试页选「从列表选择」密钥后报 `⚠ 密钥加载失败：加载失败 (500)`，密钥选不中、发不了请求。
- 根因：`/admin/keys/{id}/reveal` → `db.get_key_full` 执行 `SELECT id, key, ...`，但 `api_keys` 表只有 `key_hash` **没有 `key` 明文列**（安全策略"明文不落库"时代遗留）→ sqlite `no such column: key` → 500（无 detail）。reveal 是"明文落库"时代的死代码，与当前模型冲突。
- 决策（经用户确认）：允许明文 sk 落库回显。理由：与 `upstreams.api_key` 明文案一致，管理员即密钥持有者本就见过明文，管理后台内可"查看密钥"属标准能力。
- 改动：
  - `db.py`：SCHEMA `api_keys` 增 `key TEXT`；`_migrate_db` 对存量库 `ALTER TABLE api_keys ADD COLUMN key TEXT`（启动 init_db 自动执行）；`create_key` 落库时把明文写进 `key` 列；`get_key_by_hash` 改为显式列（**鉴权路径不把明文带出**）。模块 docstring 同步更新策略说明。
  - `admin.py` reveal：`key` 为 NULL 的旧密钥返回 **410** + detail"该密钥创建于明文回显功能上线之前，无明文可回显；请新建密钥以获取完整 sk"（不再 500）。前端 `app.js` 已有通用错误展示分支，会直接显示该 detail，无需改前端。
- 验证：复制 keys.db 到临时副本跑真实迁移：before 无 key 列 → ALTER 后 key 列在；旧行 `key=None`（旧密钥不可回显属物理限制）；新 INSERT 明文正常读回。`py_compile` 通过。
- 注意：**需重启服务**让 init_db 跑迁移（若 Docker：重建镜像重启；本机：重启 python 进程）。重启前确认旧进程已停，避免 sqlite ALTER 被占用锁死。重启后：新建密钥 → 对话测试选它即可用；老的 4 个 active 密钥无明文，只能 manual 粘贴或重建。
- 追加（09:45 收尾）：已用独立 `_e2e.db` 起 9002 测试实例跑通 HTTP 级链路：登录 → 建上游（2 次）→ 建密钥（2 次）→ `GET /admin/keys/2/reveal` **200**（明文 key 正常回显）。缓存失效参数统一升到 `?v=20260903c`（index.html 5 处），e2e 日志确认带参资源全部 200。`py_compile`（db/admin/auth/proxy/schemas/compat）与 `node --check app.js` 均通过。

## 上游协议类型(protocol) + 可用模型(models) + 对话页模型候选（同一批改动，git 工作区）
- 背景：网关同时透传 OpenAI 方言与 Anthropic 方言客户端（compat.py 翻译链），上游需标注自身协议以便路由/翻译。对话测试页需要"该上游有哪些模型"做候选下拉。
- 后端：`db.py` SCHEMA `upstreams` 增 `protocol TEXT NOT NULL DEFAULT 'openai'`、`models TEXT NOT NULL DEFAULT '[]'`；`_migrate_db` 对存量库 `ALTER TABLE ... ADD COLUMN`；`_models_to_text/_models_from_text` 序列化（兼容 JSON 数组与脏数据）；list/get/insert/update 显式列；`schemas.py` Upstream 增 `protocol: str = "openai"` 与 `models: list[str]`；`admin.py` 建/改上游时透传两字段。
- 前端：`index.html` 上游表新增「协议/模型」列（colspan 6→8）；建/改弹窗新增「协议类型」下拉（OpenAI/Anthropic）与「可用模型」textarea（一行一个）；对话测试页模型输入改为 input+datalist（id=chat-model-list），placeholder 提示可从下拉选或手输。`app.js`：`updateChatModelList()` 聚合所有上游 models 去重填充 datalist；`applyUpstreamModelsToChat(keyId)` 在切换密钥时按 `upstreamOfKey`（密钥绑定上游，无绑定回退默认上游）自动填首个模型，手动输入不被覆盖；`fillChatModelDefault` 兜底。styles.css 适配 datalist/textarea。
- e2e（09:45-09:46，9002）：建上游/建密钥/reveal 全 200，静态资源带 `?v=20260903c` 均 200。
- 状态：**正式服务 9000 仍是改动前旧进程**——新代码与 keys.db/upstreams 迁移需重启才生效；9002 e2e 测试进程（PID 24268，用 _e2e.db）仍在监听，待清理。残留清理项：`proxy/data/_e2e.db`、`_e2e_models.mjs`、`_e2e_server.log`、`.workbuddy/tmp_test_compat.py`。

## 密钥管理操作列新增「重置」（丢失/泄露一键止血）
- 需求：用户"密钥丢了也没关系"——操作列加**重置**：生成新明文替换旧 key，旧值立即失效，新明文回显一次。
- 改动：
  - `db.py` 新增 `reset_key(key_id)`：`UPDATE api_keys SET key_hash/key/key_prefix = 新值 WHERE id=? AND status='active'`，保留 id/name/upstream/统计/created_at/expires_at（同一把钥匙换锁芯）；返回含新明文 key 的记录供回显；密钥不存在或已吊销返回 None。模块 docstring 补重置策略。
  - `admin.py` 新增 `POST /admin/keys/{key_id}/reset`（admin 权限，404 detail="密钥不存在或已吊销"），返回 record（含明文 key）。
  - `app.js`：`renderKeys` 操作列 active 行渲染「重置」(ghost) +「吊销」(danger) 两按钮；新增 `resetKey(id)`（confirm 三行说明 → POST → 弹明文框 → refreshAll）；抽出 `showPlainKeyModal(keyText, mode)` 供新建/重置共用，动态切标题与提示；create-submit 分支改为调用它。
  - `index.html`：`modal-key` 标题/提示加 `id="modal-key-title"` / `id="modal-key-warn"`（新建/重置复用同一弹窗）。
  - `styles.css`：`.table td .btn + .btn { margin-left: 8px }`（操作列双按钮间距）。
  - 版本号统一 bump `?v=20260903b → 20260903c`（index.html 内 styles/app.js/vendor 四处）。
- 验证：真实 keys.db 副本上 `reset_key`：返回新 sk 明文、prefix 更新、id/name/upstream/created_at 不变；新明文哈希可被 `get_key_by_hash` 命中（网关鉴权可用）；吊销后/不存在 reset 返回 None；`py_compile`+`node --check` 通过。副本已删。
- 收益：解决了旧密钥 `key=NULL` reveal 410 的尾巴——不必新建，直接重置即可拿到一把新明文；已泄露的 key 重置后立即失效。
- 注意：reset 后若该 key 正被对话测试页选中，前端 `_chatSelectedKey` 内存里的旧明文已失效，需重新选择该密钥触发 reveal 拉新明文。
## 新建密钥：过期时间改为时间选择框
- 需求："过期时间不能手输，用时间选择框，允许为空"。
- 改动：`index.html` modal-create 过期字段由 `text` 改 `input type="datetime-local"`（留空=永不过期，label 文案同步更新）；`app.js` create-submit 对 datetime-local 无秒的 value（`YYYY-MM-DDTHH:mm`）正则补 `:00` 再 `new Date`（本地时区）→ `toISOString()` UTC 落库，时区语义与原来一致；版本号 bump `?v=20260903c → d`。

## DeepSeek anthropic 上游对话测试 502 ConnectError + 非流式响应翻译方向写反
- 用户场景：key 6 绑定 deepseek 上游（base `https://api.deepseek.com/anthropic`、protocol=anthropic、models deepseek-v4-flash/pro），对话测试页 OpenAI 方言请求 `POST /v1/chat/completions` → 502 `{"detail":"上游请求失败: ConnectError"}`。
- 根因 A（连接）：网关跑在 **pyenv 3.10.0**，其 httpx `AsyncClient()` 默认 `trust_env=True` 时本机**所有 HTTPS 出站必然 ConnectError('')**（deepseek/baidu 都失败、HTTP 正常）；`trust_env=False` 直连正常（401/200）。3.14 解释器无此问题。proxy.py 三处 httpx 客户端（`_stream_upstream`/`_forward_translated`/health）都没设 trust_env → 撞坑。
- 根因 B（翻译方向）：修好连接后返回 200 但 body 是 `type:"message"` + `content:[]` + usage 全 0——`_forward_translated` 非流式正常响应分支里两个方向的函数名写反：entry=openai/exit=anthropic 时把上游 Anthropic 响应喂给了 `openai_to_anthropic_resp`（它按 choices 取数，拿到 anthropic 形状→产出空内容假响应）。流式分支和错误分支方向是对的，所以之前流式 e2e 没暴露。
- 修复（proxy.py）：
  1. 三处 `httpx.AsyncClient(...)` 全部加 `trust_env=False`（注释说明 pyenv3.10 复现结论）；
  2. 非流式正常响应映射改为：entry anthropic&exit openai → `openai_to_anthropic_resp`；entry openai&exit anthropic → `anthropic_to_openai_resp`；
  3. 新增 `_upstream_error_detail(exc)`，502 detail 带上底层异常文本（截断 300），替代原来只有类名的写法（4 处调用点替换）。
- 验证（9003 临时实例 + 正式库副本 + 真实 DeepSeek，pyenv 3.10 跑）：stream=false 返回标准 OpenAI chat.completion（content 正常、usage 有值）；stream=true 返回 OpenAI SSE 分块 + `[DONE]`；非法模型返回 OpenAI 形状错误体且错误文案确认合法模型即 `deepseek-v4-pro`/`deepseek-v4-flash`（另有 -vision-exp）。临时实例与副本已清理。
- 注意：**正式服务 9000（PID 40648）仍需重启**才加载 proxy.py 修复。DeepSeek anthropic 端点探活仍会 502（无 /health），属预期噪音。

## 为内网无互联网机器制作离线部署包（offline-kit/）
- 用户目标：在 **Windows 10 x64 内网（无外网）** 机器上部署 llm-proxy-tk 网关。
- 做法：本机 pip 按 **cp311 / win_amd64** 平台跨版本解析下载全部依赖 wheel 到 `offline-kit/wheels-win311/`（23 个包，4.3MB，版本为 2026-09 最新：fastapi 0.141.1 / starlette 1.6.0 / uvicorn 0.52.4 / httpx 0.28.1 / pydantic 2.13.5 / pydantic-settings 2.15.0 / aiosqlite 0.22.1）。`offline-kit/` 内含 requirements.txt、ASCII-only 的 install.bat（`pip install --no-index --find-links=wheels-win311 -r requirements.txt`）、README.md 部署说明。
- 兼容性验证：新 Starlette 1.6 已删除 on_event/on_startup/on_shutdown，但 **FastAPI 0.141 自带兼容层**（routing.py 注释 "Handle on_startup/on_shutdown locally since Starlette removed support"），项目 `app/main.py` 的 `@app.on_event("startup")` 仍可用（启动打 deprecation 警告）。已在临时 venv（py3.13 + 最新依赖）真实启动冒烟：/api/info 200、/admin/login 返回 JWT、建库建默认管理员正常；/health 对无上游报 502 属预期。临时 venv 与测试进程已清理。
- 部署要点（写给用户）：目标机 Python 3.11 x64（Win10 无需 SHA-2 补丁、无需单独 VC 运行库）；`.env` 的 VLLM_TARGET_URL 填**内网上游**地址；必须在 proxy/ 目录内运行（.env 与 data 相对路径）；NSSM 服务化命令见 offline-kit/README.md；管理界面前端库全本地打包、无外网 CDN，离线可用；静态资源 https?:// 仅 vendor 注释与占位符。
- 环境备忘：本机 `python`=3.13.14 / pip 26.1.2；Git Bash 里给 native python 传路径必须用 Windows 风格（`E:/...`），POSIX `/e/...` 会报文件不存在；`python -m venv /tmp/x` 会把 venv 建到当前盘符根下（E:/tmp/x）而非 Git 的 /tmp；taskkill 在 git-bash 传 `//F` 无效，需用 PowerShell `Stop-Process`。


## README.md 同步最近功能改动（9-03 批量）
- 需求："基于修改完善最新修改的内容补充进 README.md"。
- 修正过时表述：删掉"明文密钥不落库"两处（项目简介核心价值 + 安全说明），改为"哈希鉴权 + 明文回显"策略；功能特性/对话描述同步新版本。
- 新增内容：核心价值与特性补"一键重置、方言透传、Markdown 渲染、消息工具条"；Web 管理界面表补协议/模型列；新增「### 密钥管理」小节（创建/查看明文/重置/吊销，注明写操作需管理员——create/reveal/reset/revoke 全在 `_admin_router`）；「对话界面使用」扩为 Markdown 渲染/消息工具条/生成状态 + 模型选择（下拉聚合全部上游 models，可键入不被覆盖）；多上游路由新增「### 上游字段：协议类型与可用模型」小节；API 表补 `POST /admin/keys/{id}/reset`、reveal 注明旧密钥 410；发放密钥警告改为"重置可换发"；技术栈表加前端渲染行（marked/highlight.js/DOMPurify 本地 vendor）；项目结构 static/ 展开注释含 vendor/。
- 权限事实核对：密钥列表操作列按钮仅 `isAdmin()` 渲染（app.js:392）；对话页「从列表选择」reveal 调用无前端 admin 判断但后端 403。

## offline-kit/README.md 增强为完整离线部署手册
- 用户要求把"如何离线部署安装"正式写进 offline-kit 的 README（原有版本较简）。
- 重写为七节部署手册：包结构、前置准备清单、目标机 7 步部署（装 Python→拷文件→install.bat 一键装依赖→配置 .env→手动试跑验收→NSSM/任务计划程序服务化→防火墙放行）、故障排查表、重新生成离线包方法、日常维护、依赖版本与兼容性说明。
- 核对修正：`.env.example` 其实**没有 MASTER_KEY**，代码里 master_key 定义后也从未被引用（grep 仅命中 config.py 定义与 .pyc），故从必改配置表中移除该项，避免误导。config.py 其余字段（jwt_secret/jwt_expire_hours/default_admin_username/default_admin_password/vllm_target_url/upstream_api_key/proxy_host/proxy_port/db_path）与 README 表格一致。

## 安全排查：data 目录明文密钥 + 公开仓库已泄露 .env
- 用户问："data 目录有风险吗，外部 API key 是否存储进去了"。
- 事实核查（只读查询真实 keys.db + git）：
  1. `upstreams.api_key` **明文落库**——当前库 deepseek 上游存有真实 sk-（工作区，未提交）。
  2. `api_keys.key` 明文列已存 4 把 sk-（工作区未提交）；旧 1/2 号 key 为 NULL。
  3. `admin_users` 密码为 pbkdf2 哈希；`usage_logs` 只存脱敏 prefix——安全。
  4. HTTP 不暴露：main.py 只 mount static/，data/ 不在 web 根。
  5. **高危**：仓库 remote=github.com/boonya-hrgk/llm-proxy-tk（**公开**，api 探测 200），且 git 跟踪了 `proxy/.env` + `proxy/data/keys.db`：
     - ef75a2a/836ba5d（8-14）已 push 的 .env 含真实 `JWT_SECRET` + `DEFAULT_ADMIN_PASSWORD` → **已在公开仓库泄露**，需轮换。
     - 历史 keys.db 为空表占位（无敏感），但**当前工作区 keys.db 改动未提交**——若 `git add -A` 推送则 5 份明文密钥全公开。
     - `proxy/data/_e2e.db/_e2e.log/_e2e_models.mjs` 测试残留未清理（未跟踪）。
- 处置建议（待用户确认）：加 .gitignore（proxy/.env、proxy/data/、*.db）、`git rm --cached` 停止跟踪；轮换 JWT_SECRET/默认管理员密码/deepseek 上游 key；重置/吊销现有 sk-；清理 _e2e 残留；公开仓库历史已含 .env 可考虑 filter-repo 清理或转 private。

## 恢复误删的 proxy/.env
- 现象：用户报本地 proxy/.env 没了。排查发现 HEAD(bf2b971) 树中本就不含 .env（该提交相对父提交删除了它），磁盘文件亦丢失，git status 无感知（staged 删除态与工作区一致）。
- 处置：`git show 836ba5d:proxy/.env > proxy/.env` 恢复，与 ef75a2a 版本 cmp 一致（842 字节，含 VLLM_TARGET_URL / JWT_SECRET / DEFAULT_ADMIN_USERNAME / DEFAULT_ADMIN_PASSWORD / PROXY_HOST / PROXY_PORT 等）。文件在 .gitignore 内不会再被跟踪。
- 注意：恢复的是 8-14 最后提交版本；若用户其后手动改过 .env 未提交，该改动不可恢复。且此版本含已在公开仓库历史暴露的 JWT_SECRET/默认密码，生产应视为已泄露需轮换（此前记忆有记录）。

## keys.db 真实数据丢失事故根因（11:02 分支切换）
- 用户问"切回 pro 分支数据能回来吗"。核实：
  - reflog：11:01 checkout main（836ba5d）→ 11:02 merge origin/pro fast-forward → HEAD 到 bf2b971。
  - bf2b971（今天 10:52 用户自提交，pro/main 同指向）**主动删除 proxy/.env 与 proxy/data/keys.db**（.gitignore 同提交新增）。
  - fast-forward merge 时 git 将"新 HEAD 不存在的 tracked 文件"从工作区物理删除 → .env 与真实 keys.db 一起没了（.env 已从 836ba5d 恢复）。
  - 真实 keys.db（deepseek 上游明文 api_key + 4 把明文 sk- + usage_logs）**从未提交**；git 全历史只有 8-14 空占位库（836ba5d），悬空对象中 36KB sqlite blob（eefcf549）为无表空壳 → **git 层无法恢复**。
  - 当前 data/keys.db 是 11:10 服务访问路径时自动新建的种子空库（0 api_keys、1 个默认上游无 key），非旧数据。
- 结论答复：切回 pro(=main=bf2b971) 文件不会回来；退到 836ba5d 也只有 8-14 空占位。唯一希望是 Windows 文件历史/卷影/磁盘恢复工具（文件 11:01-11:10 间被覆盖删除，扇区可能被种子库复用，概率低）。
- 待用户决定：尝试系统级恢复 or 直接重建（上游 base_url 在 .env VLLM_TARGET_URL；deepseek api_key 需控制台重置；sk- 后台新建/重置；管理员由 .env 重建）。

## 对话页"从列表选择"报 410：实为过期页面缓存（数据已重建）
- 用户报 410 时我实测：当前 proxy/data/keys.db 已有用户 11:16-11:17 重建的 2 密钥（id=1 deepseek/upstream3、id=2 ollama/upstream2，**key 列均有明文**），upstreams 3 行（默认上游/ollama/deepseek-anthropic 含明文 api_key sk-26e85c…），admin_users admin。
- GET /admin/keys 返回正常；POST /admin/keys/1/reveal 与 /2/reveal 均 **HTTP 200** 返回真实明文（sk-Vk2-TDaU1… len=35）。
- 结论：后端与数据无恙，410 源于浏览器仍持有早前（空库/旧密钥）加载的页面与缓存下拉。指引用户 Ctrl+F5 强刷后再从列表选择；仍复现则要求提供选中的密钥名 + Network 中 reveal 请求响应。
- 前端链路（app.js:120-139 switchView chat→loadChatKeys→自动选 activeKeys[0]→reveal; app.js:1089 change→reveal），密钥管理创建后 refreshAll 会刷新 loadKeys 缓存。

## 对话页仍报 410：根因=浏览器陈旧缓存，已加 no-store 防御
- 用户强刷后仍报 410。实测对照：磁盘 keys.db（2 密钥均有明文）↔ curl /admin/keys ↔ reveal 全 200，全一致；headless Chrome 走真实 iframe 完整路径（注入 sessionStorage token → switchView('chat') → loadChatKeys 自动选中首 active → reveal）3 项 PASS。结论：服务端与前端逻辑均健康，410 只可能来自浏览器缓存的旧列表/旧 reveal 响应（数据期间该 id 曾无明文）。
- 过程中发现 id=1 在 11:30-11:37 被用户重置过（prefix 由 sk-Vk2-T → sk-nJFkS），佐证用户网页端一直在操作。
- 改动（本次）：
  1. `proxy/app/main.py` 加 `@app.middleware("http")`：`/admin*` 响应统一 `Cache-Control: no-store`（**需重启服务生效**），杜绝 /admin/keys、reveal 等 GET 被启发式缓存。已补 Request 导入。
  2. `proxy/app/static/app.js` reveal 410 分支：文案改为引导型，并追加「去密钥管理重置」按钮（switchView('keys')），不再展示死胡同提示。
  3. index.html 版本号 v=20260903e → f。
- 验证：node --check / py_compile 通过；headless 复验 3 PASS 后删除 _test_reveal.html。
- 注意：静态前端改动强刷即生效；no-store 中间件需重启进程才加载（服务现仍为旧进程无中间件）。

## 11:45 用户"重置了还是 410" → 实测服务端全健康，锁定浏览器旧缓存命中
- 用户前两问："是不是生成环节没改 所以要执行重置才生效？"、"重置了也无效 还是同样提示"。
- 实测证据链（PID 9716, 端口 9000, **进程已是最新代码**）：
  1. 磁盘 keys.db：id=1(deepseek, sk-OzPrN,key_len=35)、id=2(ollama, sk-gz_WK,key_len=35) 均有明文；id=1 前缀又变（sk-nJFkS→sk-OzPrN），用户 11:42 再次重置过。
  2. 登录成功响应头已含 `cache-control: no-store` → 中间件已生效（memory 138 行"旧进程无中间件"已过时）。
  3. `/admin/keys` 200 + no-store；`reveal 1/2` 均 200 且返回完整明文。
- 结论：**生成环节本就写明文**（db.py:214 INSERT key），重置仅为补旧记录明文；当前服务端不可能再 410。用户仍见 410 = fetch 命中浏览器缓存的旧 reveal 410（GET 同 URL 未真正请求后端）；no-store 清不掉已存缓存，需一次无痕窗口/Ctrl+F5。已指导用户无痕验证 + Network 面板看 reveal 是否 "from disk cache"。
- 备忘：curl 登录返回字段是 `access_token` 非 token；登录/健康检查接口返回头 confirm 进程代码版本是快捷手段（/admin* 有 no-store = 新代码）。

## 密钥管理新增「编辑」改名（仅改名称，统计联动）
- 需求：密钥管理操作列加「编辑」按钮，仅能改名称；用量统计随改名联动展示。
- 后端：schemas `KeyRenameRequest(name)`；db `rename_key()` 只 UPDATE api_keys.name；admin 新增 `PATCH /admin/keys/{id}`（admin 权限，空串归一 null，返回 KeyListItem）。
- 统计联动关键：`get_usage_by_key` 原按 `usage_logs.key_id+key_prefix+key_name` 分组（快照名），现改 `LEFT JOIN api_keys k ON k.id=l.key_id GROUP BY l.key_id`，key_name 取 keys 当前 name → 改名后历史日志不拆条、统一显示新名。
- 前端：keys 表格操作列恒显「编辑」（active 另有重置/吊销，revoked 仅编辑）；`_keysCache` 缓存供弹窗回填；新模态 `modal-key-rename`（key-rename-*）；保存走 PATCH 后 closeModal+refreshAll。样式加 `.muted-key`。静态版本 v=20260903f→g。
- 验证：9003 临时实例（副本正式库）PATCH 改名→列表与 stats/by_key 均显示新名且 call_count 合并；空格→null；404 正常。已清理。
- 遗留：需用户重启 9000 加载；9002 e2e 进程(PID 24268)+`_e2e.db`/`_e2e_models.mjs`/`_e2e_server.log` 残留未清。

## 13:35 无痕窗口验证通过 → 顶栏加「清缓存」按钮 + api() 前端层免疫缓存
- 用户无痕模式验证 OK，坐实浏览器旧缓存命中 410。问"为什么这么多缓存"并要清缓存按钮。
- 缓存多的原因：开发服务器此前无任何 Cache-Control/ETag，浏览器对 GET（含 XHR/fetch）按启发式缓存（RFC 7234 允许 200/404/410 等无显式头时存储）；迭代期间接口语义变了但 URL 没变 → 旧响应被复用。
- 改动：
  1. `app.js` api()：fetch 默认 `cache: "no-store"`（Object.assign({cache:"no-store"}, opts, {headers})，可被调用方覆盖）→ 前端层永久免疫陈旧 HTTP 缓存，与服务端 no-store 双保险。
  2. `index.html` topbar 加 `.topbar-actions` 容器 + `#clear-cache-btn`（btn btn-ghost btn-sm「清缓存」）：点击清 CacheStorage（caches.keys/delete）→ sessionStorage 置 `cache_cleared_toast` → location.reload()；重载后 toast「浏览器缓存已清理」。
  3. `styles.css` 补 `.topbar-actions { display:flex; gap:8px; align-items:center; }`；版本号 v=20260903g→h。
- 验证：node --check 通过；curl 确认 9000 服务返回页面含按钮且引用 v=20260903h、app.js 200。纯静态改动无需重启，强刷一次即可见。

## 13:50 对话页模型下拉"只能看到一个"复盘：自动填充值触发菜单过滤
- 现象：选中的密钥所属上游配了多个模型，打开模型下拉却只显示当前一个。
- 根因：`modelMenuRender()` 无条件按输入框现值过滤候选（`q ? ordered.filter(includes q) : ordered`）；而 `applyUpstreamModelsToChat` 切密钥时会自动把上游第一个模型填进输入框 → 打开菜单时 q 非空 → 其它模型全被过滤。上次自绘下拉改造只解决了 datalist 的前缀过滤问题，漏了这层"自动填充值也参与过滤"。
- 修复：`modelMenuRender(filterByInput = true)` 增参——**打开菜单时（modelMenuOpen）传 false 显示全部候选**；仅用户键入（input 事件，默认 true）才按输入过滤；updateChatModelList/fillChatModelDefault/applyUpstreamModelsToChat 三处数据刷新重渲染也统一 false。
- 验证：临时页 `_test_picker2.html`（同源 iframe 加载真实 index+app.js，sessionStorage 注入 token，`contentWindow.eval` 驱动）：模拟 2 上游 5 模型 → 自动填 deepseek-chat → 打开菜单 5 项全显（默认上游优先排序）→ 键入 deepseek 过滤剩 2 → 点击选中回填。PASS 后删除（连同上次遗留的 `_test_picker.html`）。
- 版本号 v=20260903h→i。要点：**可输入+可选的下拉控件，"打开"与"键入"是两个过滤语义**，自动填充的值不应参与打开时的过滤。

## 14:00 模型下拉"显示别的上游模型" → 候选改为按选中密钥所属上游隔离
- 现象：下拉里出现不属于当前密钥（其所属上游）的模型。不是缓存——上一版有意"全上游模型 + 当前上游优先排序"，但对话请求走的是密钥绑定的上游，别的上游模型选了也没用 → 改为隔离。
- 改动：
  1. `collectChatModels(keyId)`：keyId>0 时只取 `upstreamOfKey(keyId)`（密钥绑定的上游；未绑定/找不到回落默认上游）的 models，否则取全部。新增 `currentChatKeyId()` 统一从 #chat-key 读 keyId；`currentChatUpstream()` 复用它。
  2. `updateChatModelList()` 按当前选中密钥刷新 `_chatModelOptions`；密钥 change handler 在 `applyUpstreamModelsToChat(keyId)` 前补调 `updateChatModelList()`。
  3. 空态提示区分：选中密钥但上游无模型 → "当前密钥所属上游未配置模型，可到上游管理添加，或直接输入模型名"；未选 → 原全局文案。
- 验证：headless 驱动（同源 iframe 注入缓存+select 选项重建，注意对话视图未进入时 #chat-key 是空 select，直接赋 value 不生效，须先插 option）：未选=5 全显 / 选 deepseek=2 / 选 ollama=3 / 键入 qwen 过滤=1 / 带值重开=3 全量 / 上游无模型空态=当前密钥文案。PASS。
- 版本号 v=20260903i→j。测试文件已清理。
